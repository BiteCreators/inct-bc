import { SnakeGame } from '@/features/games/ui/SnakeGame'

export default function Snake() {
  return <SnakeGame />
  // return <SnakeGame cellsClassName={'h-8 w-8'} fieldHeight={10} fieldWidth={10} mobileMod />
}
